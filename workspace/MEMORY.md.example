# MEMORY.md - 长期记忆

## Segway 配送环境

### 已知楼宇
<!-- agent 查询到楼宇信息后在此更新 -->
<!-- 格式: - 楼宇名称 (areaId: xxx) -->

### 常用站点
<!-- agent 查询到站点信息后在此更新 -->
<!-- 格式: - 楼宇名称 > 站点名称 (stationId: xxx) -->

### 已知机器人
<!-- agent 查询到机器人信息后在此更新 -->
<!-- 格式: - 机器人昵称 (robotId: xxx, 所属楼宇: xxx) -->

## 用户偏好

- 默认语言: 中文
- 操作风格: 专业简洁，不需要过多确认
- 常用操作: <!-- agent 根据使用频率更新 -->

## 经验教训

### API 注意事项
- Segway API 认证使用 HMAC-SHA256，凭据通过环境变量 SEGWAY_ACCESS_NAME / SEGWAY_ACCESS_KEY 配置
- 写操作（创建运单、开箱等）是 POST，读操作（查询）是 GET
- 错误码 9012 = 无可用机器人，建议稍后重试
- 错误码 9001 = 楼宇不存在，需确认 areaId
- 错误码 9002 = 运单不存在，需确认 taskId

### Skill 使用提示
- 如果用户只说了楼宇名称和站点名称，优先用 segway-delivery skill（自动完成全流程）
- 如果用户提供了具体 ID，可以直接用底层 skill（segway-task-create 等）
- 所有底层 skill 都支持 --area-name / --station-name / --robot-name 参数，会自动解析为 ID
- 脚本输出 [TASK_COMPLETE] 表示操作成功，[TASK_FAILED] 表示失败，看到后立即停止

### 写操作安全确认（重要！）
- 所有写操作必须通过 `segway-stage` skill 起草，不允许直接执行
- 正确流程：查询信息 → 调用 stage_action.py stage → 告知用户等待审批 → 结束
- 用户通过审批链接（http://IP:18800/approve/task_xxx）批准后，webhook server 直接执行 API
- 大模型不参与执行过程
- 读操作（查询楼宇、站点、机器人、运单状态等）直接执行，不需要起草
- 输出 [ACTION_STAGED] 表示任务已起草等待审批
- 禁止直接调用 segway-task-create、segway-task-manage、segway-box-control、segway-delivery 的写操作命令

## 近期事件摘要
<!-- agent 定期从 memory/YYYY-MM-DD.md 提炼到这里 -->
